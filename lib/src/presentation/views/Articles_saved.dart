import 'package:flutter/material.dart';

class ArticlesSaved extends StatelessWidget {
  const ArticlesSaved({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
